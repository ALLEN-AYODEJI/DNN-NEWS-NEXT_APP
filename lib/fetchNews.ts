const fetchNews = async (
  category?: Category | string,
  keywords?: string,
  isDynamic?: boolean
) => {
  // GraphQL queries
  // Fetch function with Next.js latest caching ...
  // Sort function by images vs not images present
  // Return news articles
};

export default fetchNews;
